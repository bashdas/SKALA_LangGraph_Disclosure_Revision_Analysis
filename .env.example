# Copy this file to .env. The app loads it automatically without overriding shell variables.
DATA_MODE=fixture
LLM_MODE=fake
OPENAI_MODEL=gpt-5-mini
LLM_TIMEOUT_SECONDS=120
LLM_MAX_RETRIES=1
# Set secrets only in the local .env or a secret manager. Never commit them.
DART_API_KEY=
OPENAI_API_KEY=
OPENAI_REASONING_EFFORT=low
GRADIO_SERVER_PORT=7860
