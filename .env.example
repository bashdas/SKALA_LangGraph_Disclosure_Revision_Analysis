# Offline defaults; no credential is required for stage 0.
DATA_MODE=fixture
LLM_MODE=fake
OPENAI_MODEL=
# Set secrets only in a local .env or secret manager. Never commit them.
DART_API_KEY=
OPENAI_API_KEY=
API_BASE_URL=http://127.0.0.1:8000

